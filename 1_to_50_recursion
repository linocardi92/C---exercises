#include <stdio.h>

int recursion (int n);

int main(void)
{
    int n = 1;
    recursion(n);
}

int recursion (int n)
{
    if (n <= 50)
    {
        printf("%i ", n);
        return recursion(n + 1);
    }
    return 0;
}
